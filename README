This repository holds files that you typically want on all computers.
The first commit has .bashrc, .vimrc, and ssh_config (.ssh/config)
files. Simply create symbolic links to each files:
    ln -s <complete_path_to_file> ~/<file>
